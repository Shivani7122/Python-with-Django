Django>=4.2,<5.0

# Database (optional - if using MySQL)
mysqlclient>=2.2.0

# Environment variables
python-dotenv>=1.0.0

# Forms & utilities
django-crispy-forms>=2.1

# API support (optional but recommended)
djangorestframework>=3.14.0

# CORS handling (if using frontend separately)
django-cors-headers>=4.3.0

# Production server
gunicorn>=21.2.0

# Static files handling (for deployment)
whitenoise>=6.6.0
